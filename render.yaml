services:
  - type: web
    name: pageindex-mcp
    env: node
    buildCommand: npm run build:production
    startCommand: node build/index-http.js
    envVars:
      - key: CLIENT_TYPE
        value: production
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
